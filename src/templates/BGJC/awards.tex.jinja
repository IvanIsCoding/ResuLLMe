%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\resheading{\VAR{heading}}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{itemize}[leftmargin=*]
    \BLOCK{for award in awards}
	\item[]
	      \award
	      {\BLOCK{ if award.title }\VAR{award.title}\BLOCK{ endif }}
	      {\BLOCK{ if award.awarder }\VAR{award.awarder}\BLOCK{ endif }}
	      {\BLOCK{ if award.date }\VAR{award.date}\BLOCK{ endif }}
	      {\\\BLOCK{ if award.summary }\VAR{award.summary}\BLOCK{ endif }}
    \BLOCK{ endfor }
\end{itemize}