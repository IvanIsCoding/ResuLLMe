\NewPart{\VAR{heading}}{}
\BLOCK{ for project in projects }
\ProjectEntry{\BLOCK{ if project.name } \VAR{project.name} \BLOCK{ endif }}{\BLOCK{ if project.url } \VAR{project.url} \BLOCK{ endif }}
{\BLOCK{ if project.keywords }
        {\VAR{project.keywords[0]}\BLOCK{ for keyword in project.keywords[1:]}, \VAR{keyword}\BLOCK{ endfor } } 
    \BLOCK{ endif }}
{\BLOCK{ if project.description } \VAR{project.description} \BLOCK{ endif }}
\BLOCK{ if not loop.last} \sepspace \BLOCK{ endif }
\BLOCK{ endfor }